Java library supporting the creation and parsing of RTML (Remote Telescope Markup Language) documents, and their conversion into a DOM (document object model).

Dependacies as follows:
- org.estar.astrometry
- org.estar.cluster
